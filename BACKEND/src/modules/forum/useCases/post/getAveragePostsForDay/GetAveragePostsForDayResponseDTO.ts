/**
 * Represents the get the average posts per user for a specific day response data transfer object (DTO).
 */
export interface GetAveragePostsForDayResponseDTO {
  // Gets or sets the average posts per user.
  average: Number;
}
