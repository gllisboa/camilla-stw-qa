/**
 * Represents the get the the posts with comments made by the member request data transfer object (DTO).
 */
export interface GetPostsWithMyCommentsRequestDTO {
  // Gets or sets the user id responsible for the request.
  userId?: string;
}
