/**
 * Represents the get all posts ascending request data transfer object (DTO).
 */
export interface GetAllPostsAscendingRequestDTO {
  // Gets or sets the user id responsible for the request.
  userId?: string;
}
